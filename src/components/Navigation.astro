---
interface Props {
  currentPage: string;
}

const { currentPage } = Astro.props;
---

<nav class="relative">
  <!-- Header with Logo and Hamburger -->
  <div class="flex items-center justify-between">
    <!-- Logo/Brand -->
    <a href="/" class="flex items-center space-x-3 hover:opacity-80 transition-opacity">
      <div class="w-10 h-10 bg-gradient-to-br from-purple-600 to-teal-600 rounded-full flex items-center justify-center shadow-md">
        <span class="text-white font-bold text-lg">KR</span>
      </div>
      <div>
        <h1 class="text-xl font-bold text-gray-800">Karen Rabins, LCSW</h1>
        <p class="text-sm text-purple-600">Licensed Clinical Social Worker</p>
      </div>
    </a>
    
    <!-- Desktop Navigation -->
    <div class="hidden md:flex items-center gap-6">
      <a href="/" class={currentPage === 'home' ? 'text-purple-700 font-semibold border-b-2 border-purple-700 pb-1' : 'text-gray-700 hover:text-purple-700 transition-colors font-medium'}>Home</a>
      <a href="/about" class={currentPage === 'about' ? 'text-purple-700 font-semibold border-b-2 border-purple-700 pb-1' : 'text-gray-700 hover:text-purple-700 transition-colors font-medium'}>About</a>
      <a href="/services" class={currentPage === 'services' ? 'text-purple-700 font-semibold border-b-2 border-purple-700 pb-1' : 'text-gray-700 hover:text-purple-700 transition-colors font-medium'}>Services</a>
      <a href="/what-to-expect" class={currentPage === 'what-to-expect' ? 'text-purple-700 font-semibold border-b-2 border-purple-700 pb-1' : 'text-gray-700 hover:text-purple-700 transition-colors font-medium'}>What to Expect</a>
      <a href="/ketamine-therapy" class={currentPage === 'ketamine' ? 'text-purple-700 font-semibold border-b-2 border-purple-700 pb-1' : 'text-gray-700 hover:text-purple-700 transition-colors font-medium'}>Ketamine Therapy</a>
      <a href="/contact" class={`bg-gradient-to-r from-purple-600 to-teal-600 text-white px-5 py-2.5 rounded-full hover:shadow-lg transform hover:scale-105 transition-all duration-300 font-medium ${currentPage === 'contact' ? 'ring-2 ring-purple-300' : ''}`}>Contact</a>
    </div>
    
    <!-- Mobile Hamburger Button -->
    <button id="mobile-menu-button" class="md:hidden p-2 text-gray-700 hover:text-purple-700 focus:outline-none" aria-label="Toggle mobile menu">
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
      </svg>
    </button>
  </div>
  
  <!-- Mobile Menu Overlay -->
  <div id="mobile-menu" class="hidden md:hidden absolute top-full left-0 right-0 bg-white shadow-xl border border-purple-100 rounded-2xl mt-2 z-50">
    <div class="flex flex-col p-4 space-y-3">
      <a href="/" class={currentPage === 'home' ? 'text-purple-700 font-semibold py-2 px-3 bg-gradient-to-r from-purple-50 to-teal-50 rounded-lg' : 'text-gray-700 hover:text-purple-700 hover:bg-purple-50 transition-colors font-medium py-2 px-3 rounded-lg'}>Home</a>
      <a href="/about" class={currentPage === 'about' ? 'text-purple-700 font-semibold py-2 px-3 bg-gradient-to-r from-purple-50 to-teal-50 rounded-lg' : 'text-gray-700 hover:text-purple-700 hover:bg-purple-50 transition-colors font-medium py-2 px-3 rounded-lg'}>About</a>
      <a href="/services" class={currentPage === 'services' ? 'text-purple-700 font-semibold py-2 px-3 bg-gradient-to-r from-purple-50 to-teal-50 rounded-lg' : 'text-gray-700 hover:text-purple-700 hover:bg-purple-50 transition-colors font-medium py-2 px-3 rounded-lg'}>Services</a>
      <a href="/what-to-expect" class={currentPage === 'what-to-expect' ? 'text-purple-700 font-semibold py-2 px-3 bg-gradient-to-r from-purple-50 to-teal-50 rounded-lg' : 'text-gray-700 hover:text-purple-700 hover:bg-purple-50 transition-colors font-medium py-2 px-3 rounded-lg'}>What to Expect</a>
      <a href="/ketamine-therapy" class={currentPage === 'ketamine' ? 'text-purple-700 font-semibold py-2 px-3 bg-gradient-to-r from-purple-50 to-teal-50 rounded-lg' : 'text-gray-700 hover:text-purple-700 hover:bg-purple-50 transition-colors font-medium py-2 px-3 rounded-lg'}>Ketamine Therapy</a>
      <a href="/contact" class="bg-gradient-to-r from-purple-600 to-teal-600 text-white px-4 py-3 rounded-full hover:shadow-lg transition-all duration-300 font-medium text-center">Contact</a>
    </div>
  </div>
</nav>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const menuButton = document.getElementById('mobile-menu-button');
    const mobileMenu = document.getElementById('mobile-menu');
    
    if (menuButton && mobileMenu) {
      menuButton.addEventListener('click', function() {
        mobileMenu.classList.toggle('hidden');
      });
      
      // Close menu when clicking outside
      document.addEventListener('click', function(event) {
        if (!menuButton.contains(event.target) && !mobileMenu.contains(event.target)) {
          mobileMenu.classList.add('hidden');
        }
      });
    }
  });
</script>